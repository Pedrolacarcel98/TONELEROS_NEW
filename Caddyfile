toneleros.in {
    # Servir el Frontend (React en puerto 3000)
    handle {
        reverse_proxy frontend:3000
    }

    # Redirigir la API al Backend (FastAPI en puerto 8000)
    handle_path /api* {
        reverse_proxy backend:8000
    }

    # Optimización de carga
    encode zstd gzip
}
